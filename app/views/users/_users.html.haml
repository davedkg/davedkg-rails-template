- @users.each do |user|
  %tr
    %td
      = user_name(user)
    %td.d-none.d-md-table-cell
      = user.email
    %td.d-none.d-md-table-cell
      = user.role
    %td.text-right
      - if policy(user).show?
        = link_to user_path(user), class: "btn btn-secondary btn-sm" do
          %i.fa.fa-eye
          .d-none.d-sm-inline
            Show