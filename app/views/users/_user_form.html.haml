= simple_form_for(@user, remote: true) do |f|
  = f.error_notification
  = f.input :name
  = f.input :time_zone
  = f.input :avatar, image_cropper: true
  = f.button :submit, "Update User", data: { "disable-with": "Updating User..." }
  = link_to "Cancel", @user, class: "btn btn-default"