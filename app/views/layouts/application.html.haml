!!!
%html{ data: { bs_theme: "light", sidebar_toggled: "no" } }
  %head
    :javascript
      document.documentElement.dataset.sidebarToggled = localStorage.getItem("SIDEBAR_TOGGLED") || "no"
    = render 'head'
  %body{ data: { controller: "sidebar" } }
    = render 'sidebar'
    #main
      = render 'navbar'
      = render 'flash'
      = yield
    = turbo_frame_tag :modal
