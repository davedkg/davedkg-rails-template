!!!
%html{ data: { bs_theme: "light", sidebar_toggled: "no" } }
  %head
    = render 'head'
  %body
    .main-container
      = render 'sidebar'
      .body-container
        = render 'navbar'
        .scrollable-body-container
          = render 'flash'
          = yield
    = turbo_frame_tag :modal